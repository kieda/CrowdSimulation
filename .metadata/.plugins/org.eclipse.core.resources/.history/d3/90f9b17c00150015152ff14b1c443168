package edu.cmu.cs464.p3.modulelang.ast
import java.util.{List => JList};

/**
 * @author zkieda
 */
case class ModuleImport(path: JList[ImportToken]) extends Node{
  
  def isPackageImport : Boolean = path.get(path.size() - 1).isInstanceOf[Star];
  def length = if(isPackageImport) path.size()
  def refEquals (javaRef : JavaRef) : Boolean = 
    if(isPackageImport) false 
    else javaRef.length == length && {
      for(i <- 0 until length) 
        if(javaRef.path.get(i).ne(path.get(i))) return false
      true
    }
}